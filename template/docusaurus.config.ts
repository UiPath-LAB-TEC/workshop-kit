import {createWorkshopConfig} from '@uipath-lab-tec/workshop-kit/config';

// Title, tagline and repo live in config/workshop-targets.json so the CLI reads
// the same values. Pass overrides here only for something genuinely unique to
// this workshop.
export default createWorkshopConfig();
